# Docker

La configurazione principale e consigliata si trova nel file `docker-compose.yml` alla radice del repository.

Da questa cartella esiste anche un Compose alternativo, utile se vuoi lanciare i servizi da `docker/`:

```powershell
docker compose up --build
```
