# Frontend

Web app React/Vite per l'avvio locale del Mediacon Hub ERP.

## Avvio rapido Windows

Dal repository principale:

```powershell
scripts\start-frontend.bat
```

Oppure manualmente:

```powershell
cd frontend
npm install
npm run dev
```

La web app risponde su http://localhost:5173.
